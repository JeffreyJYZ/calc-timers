{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window on desktop platforms (Windows, macOS, Linux).",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:default", "store:default"]
}
