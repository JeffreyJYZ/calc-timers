{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Capability for the main window on mobile platforms (iOS, iPadOS, Android).",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:default", "store:default"]
}
